import { Component } from '@angular/core';
import { HomeComponent } from './home/home.component'; // Import the HomeComponent

@Component({
  selector: 'app-root',
  template: `<app-home></app-home>`, // Set your default component here
})
export class AppComponent {}
